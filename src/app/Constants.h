#pragma once

namespace Constants {

// Application Info
constexpr const char *APP_NAME = "TinyTools";
constexpr const char *APP_VERSION = "1.1.0";

// Maximum number of resources (tabs); enforced by the settings UI and import.
constexpr int MAX_RESOURCES = 10;

// Clipboard text longer than this is ignored.
constexpr int MAX_CLIPBOARD_TEXT_LENGTH = 100000;

} // namespace Constants
